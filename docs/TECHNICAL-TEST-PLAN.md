# 技術試験計画

## 状態の意味

- PASS: 合格
- FAIL: 不合格
- READY: 試験を実行できる状態
- BLOCKED: 実機または外部条件待ち
- PLANNED: 実装前

## 試験一覧

| ID | 試験 | 状態 | 実機 | 合格条件 |
| --- | --- | --- | --- | --- |
| T01 | .NET 10 WPFビルド | PASS | 不要 | Releaseビルドが警告なしで成功 |
| T02 | キーボード直接ショートカット | PASS | 不要 | H/M/A、アプリ一覧専用の矢印・Enter、座標を確認 |
| T03 | ADB devices解析 | PASS | 不要 | USB、Wi-Fi、unauthorizedを正しく分類 |
| T04 | ADB mDNS解析 | PASS | 不要 | `_adb-tls-connect._tcp`だけを抽出し重複排除 |
| T05 | Rokid機種判定 | PASS | 不要 | Rokid識別子だけを許可 |
| T06 | 画面サイズとIPv4解析 | PASS | 不要 | 標準出力の表記揺れを処理 |
| T07 | HUD合成 | PASS | 不要 | 出力サイズ、黒透過、緑HUDを検証 |
| T08 | Windows Graphics Capture API利用可否 | PASS | 不要 | 現在のPCでAPIが利用可能 |
| T09 | scrcpy HWND取得 | PASS | Rokid必要 | HUDとカメラのHWNDをPIDから取得 |
| T10 | 画面外scrcpyの連続取得 | PASS | Rokid必要 | 2入力を15fpsで5分以上取得 |
| T11 | RV101 USB ADB認識 | PASS | Rokid必要 | `adb devices`が`device`として表示 |
| T12 | USBから暗号化Wi-Fiへの移行 | PASS | Rokid必要 | 初回USB認証後に動的TLS接続へ移行し、`adb tcpip 5555`を実行しない |
| T13 | 背景なし操作 | PASS | Rokid必要 | マウスと現行のH/M/A、アプリ選択、Esc操作が動作 |
| T14 | 入力の隔離 | PASS | Rokid必要 | 他アプリの入力をRokidへ送らない |
| T15 | カメラ競合からの復帰 | PASS | Rokid必要 | カメラ解放後30秒以内に復帰 |
| T16 | Wi-Fi瞬断からの復帰 | PASS | Rokid必要 | 自動再接続して表示を再開 |
| T17 | 終了時クリーンアップ | PASS | Rokid必要 | PCとRokidに動作中プロセスを残さない |
| T18 | 公開前UI確認 | PASS | Rokid必要 | UI調整、複数画面での配置、実機再確認、利用者承認が完了 |

## 実機試験を依頼するタイミング

T01からT08までがPASSになり、ADBとscrcpyを含む開発用ビルドが用意できた時点で、利用者へ次を依頼する。

1. RokidのADB開発者モードを有効にする。
2. Rokidを起動する。
3. 5ピン開発用ケーブルでWindows PCへ接続する。
4. Rokid側にUSBデバッグの確認が出た場合は許可する。
5. Windowsのデバイスマネージャーに警告が出た場合は画面を共有する。

ドライバは機器IDと署名元を確認するまでインストールしない。

## 2026-07-28 実行結果

- Release直列ビルド: 成功、警告0、エラー0
- セルフテスト: 8件成功
- Windows Graphics Capture: `GraphicsCaptureSession.IsSupported() = True`
- scrcpy: 公式v4.1 Windows x64版、公開SHA-256との一致を確認
- ADB: v37.0.0を起動し、デバイス一覧取得後にサーバーを終了
- WPF起動スモークテスト: 起動、終了ログ、終了コード0を確認
- 実機未接続のため、T09以降は未実施

## ライブ映像の判定

- 解像度: Rokidの`wm size`と一致
- フレームレート: 目標15fps
- 継続時間: 最低5分
- カーソル: 映像へ含めない
- 受信用ウインドウ: デスクトップとAlt+Tabへ表示しない
- 合成: 黒HUDはカメラへ影響せず、緑と白のHUDが明るく加算される
- スライダー: 0から1の変更が即時反映される

## 2026-07-29 実機試験結果

- USB ADB: `device`として認識し、Rokid製RG-glassesであることを確認
- OSと画面: Android 12、480×640
- USBからWi-Fi ADB: 当時はTCP 5555への移行と機種再確認に成功したが、後のスマートフォンアプリ接続障害を受けて現行仕様から廃止
- 画面受信: scrcpyで10秒間の非表示録画に成功、終了コード0
- HUDとカメラの同時受信: 15fpsで5分間継続し、両方とも正常終了
- Wi-Fiのみの継続確認: USBを外した後もADB接続を維持
- カメラ再取得: 直前の受信終了直後は競合する場合があり、約5秒の待機後に再取得できることを確認
- scrcpy HWND取得: 起動直後のPIDから枠なしウインドウを検出
- Windows Graphics Capture: 枠なしscrcpyから480×640の初回フレームを86msで取得
- BGRA画素取得: 480×640、1,228,800バイトの読み出しとチェックサム算出に成功
- 実画面合成: HUDとカメラを同時取得し、480×640のスクリーン合成に成功
- 合成性能: 480×640、HUD太さ最大の平均処理時間24.8ms
- 2本目のscrcpy初期化競合を確認し、カメラとHUDの両方へ個別再試行を実装
- 背景なしモード: Windowsアプリから実画面を表示
- キー入力: `H`と左右キーがRokidへ届くことを画面とログで確認
- Wi-Fiのみの再起動: USBを外した状態でアプリから画面表示に成功
- マウス入力: scrcpy画面のクリックがRokidへ届くことを確認
- 入力隔離: 電卓を前面にして押した`H`がRokidへ送信されないことを確認
- 互換終了: `Ctrl+Q`で終了し、クリーンアップに成功
- 終了処理: Windowsプロセス、Rokid側PID、生存信号の残存なし
- 実機固有のシリアル番号、IPアドレス、SSIDは記録しない

## 2026-07-29 追加実装・実機不要試験

- ライブ映像の実行中障害から、ADB再探索、Windows操作モード再開、画面サイズ再取得、HUD・カメラ再構築までを自動実行する処理を実装
- 下段ナビゲーション選択時の黒い外周・水色の内周リングをライブ画面と背景なし画面へ実装
- 「文字・アイコンの見やすさ」の前回値保存を実装
- GitHub ReleasesのポータブルZIPを正式な配布方針として確定
- Microsoft Artifact Signing Public Trustの地域制限を確認し、署名候補をSignPath Foundationへ変更
- Releaseビルド: 成功、警告0、エラー0
- セルフテスト: 9件成功
- 自己完結型x64パッケージ: 81,099,023バイト、425項目のZIPを生成
- パッケージ内容: アプリ本体、ADB、scrcpy、監視スクリプト、ライセンスを確認
- パッケージ版WPF起動スモークテスト: 終了コード0
- T16は実装完了によりREADYへ変更。合否判定にはRokid実機でのWi-Fi瞬断試験が必要

## 2026-07-29 再接続・選択リング実機試験

- 背景なしモード: 下キーで中央リング、右キーで右項目への移動を目視確認
- ライブ映像モード: カメラ映像とHUDの合成上に中央リングを目視確認
- カメラ競合: Android標準カメラのプレビュー開始でライブ用カメラ停止を発生
- カメラ競合復帰: カメラ終了後約6.1秒でHUDとカメラの2画面が自動復帰し、T15合格
- Wi-Fi ADB通信切断: 約16.2秒でHUDとカメラの2画面が自動復帰
- Wi-Fi無線停止: 停止状態を確認後、Rokid側監視処理が自動的に再有効化
- Wi-Fi無線停止中はUSBを安全網として接続し、約17秒で表示復帰
- Wi-Fi再有効化後にUSBを外すと、保存済みWi-Fiアドレスを再試行せず自動復帰が時間切れになる不具合を確認
- 手動再試行ではHUDとカメラの2画面がWi-Fi経由で復帰
- 原因: 直前の接続がUSBの場合、再接続処理が保存済みWi-Fiアドレスを候補に含めていなかった
- 修正: 直前の接続方式にかかわらず保存済みWi-Fiアドレスを再利用
- 回帰セルフテストを追加し、全10件が成功
- この時点では修正後の無線瞬断再試験が必要なためT16はREADY

## 2026-07-29 Wi-Fi再接続修正後の実機再試験

- 修正版をUSB未接続で起動し、HUDとカメラの2画面がWi-Fi経由であることを確認
- ライブ映像を維持したままUSBを安全網として接続
- Wi-Fi無線の停止を確認後、Rokid側監視処理が自動的に再有効化
- 約17.4秒でUSB経由のHUDとカメラが自動復帰し、手動復旧は不要
- Wi-FiのIPv4アドレスが保存済み接続先と一致していることをUSB経由で確認
- USBを外した後、ADBへ手動接続せず約2.5秒でHUDとカメラの2画面が保存済みWi-Fi経由へ自動復帰
- エラー画面や手動の「もう一度試す」は表示されず、ライブ映像を目視確認
- 正常終了後、Windowsアプリ、scrcpy、Rokid側監視処理の残存なし
- T16合格

## 2026-07-29 標準モード入力追加試験

- `H`: ウェイク、ホーム、下段中央Homeのタップをログと画面で確認
- `↓`: 下段中央の選択リング表示を確認
- `←` / `→`: 選択リングの項目間移動を確認
- `↑`: 下段選択解除とリング消去を確認
- `Enter`: 上段では`KEYCODE_ENTER`、下段では選択項目タップとリング消去を確認
- `Esc`: 上段では`KEYCODE_BACK`、下段では選択解除とリング消去を確認
- 単独`Space`: 350ms後の中央タップをログで確認
- `Ctrl+Q`: アプリ全体の正常終了とクリーンアップを確認
- `Alt+F4`: scrcpyだけが閉じて自動再接続される不具合を確認し、アプリ全体の終了操作へ修正
- 修正後の`Alt+F4`: アプリ、scrcpy、Rokid側監視処理の正常終了を実機確認
- 350ms以内の`Space`2回は自動操作で間隔を保証できないため、手動確認待ち
- T13はダブルSpaceの手動確認が残るためREADYを維持

## 2026-07-29 起動画面レイアウト修正

- 起動画面の固定高さ430では、説明と操作ボタンがウインドウ外へはみ出す場合があることを確認
- 初期サイズを560×600へ変更し、タイトル、説明、2つのモード選択、キャンセルが同時に収まることを目視確認
- 起動、接続進行、エラーの各パネルを縦スクロール対応に変更
- 説明文を折り返し対応にし、文字表示が大きい環境でも操作項目へ到達できる構成に変更
- Releaseビルド: 成功、警告0、エラー0
- T18は他のUI調整と表示倍率確認が残るためREADYを維持

## 2026-07-29 背景なし画面の入力先・×終了修正

- 利用者試験で、背景なし画面が見えていてもキーボード入力先にならない場合があることを確認
- ログでは左右キーと`H`を受信していたが、入力先が分かりにくいため、起動・再接続後に背景なし画面を自動的に前面化するよう修正
- 修正版を実際に起動し、Rokidのホーム画面とマウスポインターを目視確認
- `↓`: 中央Homeに選択リングが表示されることを目視確認
- `→`: 選択リングが右のアプリ一覧へ移動することを目視確認
- `H`: 選択リングを解除し、Home操作が送信されることを画面とログで確認
- 背景なし画面の×によるscrcpy正常終了を、通信切断と誤判定して自動再接続する不具合を修正
- 修正後の×: `exitCode=0`とRokid接続中を確認してアプリ全体を終了
- 終了後12秒間、Rokid Controlとscrcpyの再起動・残存処理がないことを確認
- 終了コードと接続状態から終了・再接続を判定するセルフテストを追加

## 2026-07-29 選択リングのMac版準拠修正

- Windows版のリングが黒く太い外周と緑寄りの内周を別々の円として描き、不自然に見えることを確認
- Mac版`NavigationHighlightView`と`VisionDisplayView`の実装を再確認
- 背景なし画面をMac版と同じ44×44、黒65%・3内側・線幅7、シアン・5内側・線幅3へ変更
- シアンはmacOSのダーク表示時の`systemCyan`相当色`#64D2FF`を使用
- ライブ映像側もMac版と同じ`max(44 × 表示倍率, 30)`および同じ線の重なりへ変更
- 修正版を背景なし画面へ表示し、黒縁とシアンが1本のリングとして重なることを目視確認
- `→`でアプリ一覧へ移動後も同じ描画と位置関係を維持することを目視確認

## 2026-07-29 選択リングの表示倍率・中心位置修正

- 固定44 DIPでは、480×640のRokid画面を約320×427へ縮小する背景なし画面上でリングだけが縮小されず、過大になることを実画面で確認
- ADBスクリーンショットから、下段Homeアイコンの表示中心が480×640座標の`(240, 330)`であることを確認
- 入力用タップ座標`(240, 320)`は維持し、選択表示のみ実際のアイコン中心へ分離
- 背景なし画面の直径・余白・線幅をすべてRokid画面の表示倍率で縮小
- 480→320表示時の期待直径は約29.3画面ピクセル
- 修正版を実機画面に表示し、Home、メモ、アプリ一覧で大きさと中心位置をスクリーンショット確認する

## 2026-07-29 キーボード入力・障害回復の再点検

- 利用者試験の「右キーでは動かず、上キーでリングが出た後に動く」を実機ADB入力と画面取得で再現
- 原因は入力欠落ではなく、H後にPC側は上段、Rokid純正フォーカスは下段Homeへ残る状態不一致
- 下段Home状態で右キーを送るとアイコン列が消え、上キーで純正上段リングが再表示されることを確認
- Hの複合入力末尾へ上キーを追加し、Home表示後にRokid側も上段へ戻すよう修正
- 下段進入時はランチャー判定を先に行い、ホーム画面では端末へ下キーを送らずPC側リングだけを表示
- Sandboxの最新版で、H後に表示が消えないこと、下キーでHomeリング、右キーでアプリ一覧リングへ即時移動することを確認
- 持続ADB入力へ起動時応答確認、命令終了コード確認、切断後の再作成、2秒のランチャー判定キャッシュを追加
- 一時的な入力失敗後も次の操作を処理する回帰テストを追加
- 外部キャンセル時の子プロセス残存を修正し、実プロセスを使う回帰テストを追加
- 表示セッションが5秒以内に2回終了した場合の自動再起動停止を実装・テスト
- ライブ映像のUI描画を最新フレームへ集約し、古いフレームの滞留を防止
- Releaseビルド: 成功、警告0、エラー0
- 通常セルフテスト: 15件成功
- Wi-Fi ADB実機入力: `KEYCODE_UNKNOWN` 2回の測定で5回平均56～66ミリ秒
- 二重起動時に既存画面へ通知する名前付きイベントを追加
- 初回実装ではWindowsの前面化制限でscrcpyが前面へ出ないことを実画面で確認
- 2つ目のプロセスから既存プロセスへ前面化権限を渡す修正を追加し、ビルド確認済み
- 終了が400ミリ秒を超えた場合の「終了しています…」表示を追加
- 二重起動と終了中表示の最新版実画面確認は、Sandbox再作成後に実施する

## 2026-07-29 キーボード操作の単純化試験

この節はリング方式の旧試験記録であり、後述の「直接ショートカット方式」に置き換えた。

- `H`で固定座標タップを行わずホームへ戻り、リングを表示しないこと
- 下段リング表示中の`H`でリングを解除し、ホームへ戻ること
- `H → 右`で`KEYCODE_DPAD_RIGHT`をRokidへ送ること
- `H → Enter`で`KEYCODE_ENTER`をRokidへ送ること
- ホーム画面の`↓`だけがHomeリングを表示すること
- リング表示中の`←` / `→`はADBへ送らず、メモ・Home・アプリ一覧のリングだけを移動すること
- 下段Homeの`Enter`は座標タップを行わずホームへ戻り、リングを解除すること
- メモ・アプリ一覧の`Enter`は対応座標を開き、リングを解除すること
- リング解除後の矢印キーが通常のRokid方向キーへ戻ること
- Releaseビルド: 成功、警告0、エラー0
- 通常セルフテスト: 15件成功
- ライブ映像の実機試験で、上記のうち`H`、`H → 右`、`H → Enter`、`H → 下`、下段Homeの`Enter`、アプリ一覧の`Enter`とリング解除を確認

## 2026-07-29 ライブ映像合成・入力回帰試験

- Mac版の合成式`1 + 11v`、太さ`1.25v`、アスペクトフィル、スクリーン合成とWindows版を再照合
- Windows版の最小値でHUDが消える相違と、輪郭が過度に太くなる相違を修正
- カメラとHUDを個別に指定出力サイズへアスペクトフィルする回帰試験を追加
- スライダー最小値でも元のHUDが表示されることを画素値と実機画面で確認
- 暗いHUDがスライダー値に応じて増幅されることを画素値で確認
- スライダー最小、中間、最大でHUDが段階的に強調されることを実機目視確認
- ライブ映像中の`H → 右`でリングを表示せず通常の右移動、`H → 下 → 右 → Enter`でHomeリング表示、Applications移動、起動後のリング解除を確認
- 設定変更後も12秒以上カメラとHUDの合成表示が継続
- Releaseビルド: 成功、警告0、エラー0
- 通常セルフテスト: 15件成功
- T07、T10、T14の回帰に合格

## 2026-07-29 キーボード直接ショートカット方式

- 下段リング、下段選択状態、ランチャー判定をWindows版から削除
- `H`は検出画面サイズから算出した下段中央Homeを直接選ぶ
- `M`はメモを直接開く
- `A`はアプリ一覧を直接開き、8秒間だけ方向キーとEnterを有効化
- アプリ一覧の外では方向キーとEnterをRokidへ送らない
- アプリ一覧では`←` / `→`を送り、`Enter`後は選択状態を終了
- `Esc`は常に`KEYCODE_BACK`を送る
- 通常時はWindows側に、Rokidの下段アイコンと同じ順の`M メモ　H Home　A アプリ`を常時表示
- アプリ一覧の選択中は`← → 選択　Enter 決定　Esc 戻る`へ切り替え
- Windows Releaseビルド: 成功、警告0、エラー0
- 単一実行ファイル版セルフテスト: 15件成功
- 通常セルフテストはWindowsの未署名DLL実行制限により読み込みを拒否されたが、同一コードを単一実行ファイル化して全件実行
- Mac参照ソースも同じH/M/A方式へ変更し、リング描画を操作案内へ置換
- Windows・Macの監視スクリプトから10秒ごとの`KEYCODE_WAKEUP`と`KEYCODE_UNKNOWN`を削除
- scrcpyの復元可能な画面休止防止へ置換
- Windows版WPF画面からのキーボード実機試験と、Mac上のコンパイル・実機試験は未実施
- 5分以上の連続表示と、Rokid側カメラ競合からの自動復帰は公開前の長時間試験として残す

### USB実機によるH/M/A操作列の確認

- `KEYCODE_WAKEUP`と`KEYCODE_HOME`でHomeへ戻ることを画面取得で確認
- 480×640の検出画面サイズから算出したメモ座標をタップし、メモへ切り替わることを確認
- 同じ画面サイズから算出したアプリ一覧座標をタップし、アプリ一覧が開くことを確認
- アプリ一覧で`KEYCODE_DPAD_RIGHT`を送り、選択が「音楽」から「翻訳」へ移動することを確認
- SandboxはホストのUSBとADB認証状態を共有しないため、WPF画面へ物理キーボードを入力する最終試験は未完了
- 端末側ADBモードを変更する`adb tcpip 5555`は使用していない

## 2026-07-29 ライブウィンドウのMac版準拠修正

- Mac版`VisionModeController`がタイトル付き・移動可能・サイズ変更可能なウィンドウを使用することを再確認
- Mac版の見やすさスライダーがタイトルバー直下の上部にあることをソースと参照画面で確認
- Windows版の下部スライダーを上部へ移動し、`薄い`、`濃い`、最大幅185の構成へ変更
- 起動画面とライブ画面を現在のWindows作業領域内へ収めて中央配置する処理を追加
- 最大化Sandboxで起動画面とライブ画面のタイトルバー、全表示、上部スライダーを目視確認
- 通常サイズSandboxでもタイトルバー、閉じるボタン、起動画面の全操作、ライブ画面、上部スライダーを目視確認
- 最大化・通常サイズの両方でタイトルバーをドラッグし、ウィンドウが移動することを確認
- Releaseビルド: 成功、警告0、エラー0
- 通常セルフテスト: 15件成功

## 2026-07-29 見やすさスライダーの実用範囲再調整

- 利用者の実機評価で、旧レンジは約30%を超えるとHUDが太くにじみ、上側約70%が実用にならないことを確認
- 新しい最小強度を旧最小値の半分に設定
- 新しい最大強度を旧レンジ約30%相当の4.3に設定
- ライブ表示から輪郭膨張を外し、スライダー全域で元の線幅を維持
- 最小値でHUDが消えず、従来最小値より薄いことを画素値で確認
- 最大値でも隣接画素へ輪郭が広がらないことを画素値で確認
- Releaseビルド: 成功、警告0、エラー0
- 通常セルフテスト: 15件成功
- Wi-Fi ADBを再接続し、最小・中間・最大の全点で濃さが段階的に変わることを実機目視確認
- 最大値でも旧レンジ上側に見られた太い縁取りやにじみが再発しないことを確認

## 2026-07-29 見やすさスライダーの横幅修正

- `MaxWidth="185"`と左寄せにより、スライダー右端と`濃い`の間へ不要な空白ができることを確認
- 最大幅制限を削除し、`薄い`と`濃い`の間の可変領域いっぱいへ伸ばすよう修正
- ツールチップから、現在は変更しない「太さ」の説明を削除
- Releaseビルド: 成功、警告0、エラー0
- 通常セルフテスト: 15件成功
- 新しい実行物は`artifacts/publish/win-x64/slider-width-next`

同時刻の接続診断では、ホスト側ADBにも接続端末がなく、mDNSの`_adb-tls-connect._tcp`通知も0件、直前のRokid IPへも到達できなかった。利用者からWi-Fi ONは開いているとの訂正を受けたため、利用者操作不足とは扱わない。WindowsのWi-Fiインターフェースを明示し、`_adb._tcp`、`_adb-tls-connect._tcp`、`_adb-tls-pairing._tcp`を直接問い合わせたが、同一LAN上のプリンターのmDNS応答は受信できる一方、RokidのADBサービス応答は0件だった。したがって、この時点ではWi-Fi自体ではなく、Rokid上のワイヤレスADB復旧が完了していない、または復旧したADBサービスをWindows向けに発見できない状態として扱う。SandboxはホストとADB接続状態を共有しないため別途問題になり得るが、ホストでもADBサービスが見えない現在の一次原因ではない。

接続障害の範囲を再確認した結果、利用者の報告対象はスマートフォンのRokidアプリだった。試験中に実行した`adb tcpip 5555`はRokid側のadbdを再起動し、Wi-Fi ONが復旧するTLS接続方式とは別の状態へ変更するため、端末全体の接続回帰に含める。以後、Sandbox接続の回避策として端末側のADBモードを変更しない。公開前試験では、Windows版終了後にスマートフォンアプリ接続、Wi-Fi ON表示、Rokid再起動後の復旧を確認する。

## 2026-07-29 H/M/A直接移動の全組合せ試験

- 最新版Windowsアプリのライブ映像で`H→H→M→M→A→A→H→A→M→H`を順に実行し、全9遷移を一巡
- 各キー入力後にRokid実画面を取得し、Home、メモ、アプリ一覧の選択位置を確認
- `A`ではPC側案内がアプリ選択用へ切り替わり、`H`または`M`で通常のH/M/A案内へ戻ることを確認
- 背景なし画面でもH/M/A案内の常時表示、A選択案内への切替、`A→H`を確認
- Releaseビルドは警告0・エラー0、セルフテスト15件成功

## 2026-07-30 案内順とマルチディスプレイ確認

- Windowsの通常案内をRokidの下段アイコンと同じ`M メモ　H Home　A アプリ`の順へ変更
- 背景なし画面で通常案内、A選択案内、`M→H`、`A→H`、A選択中の右・Enterを実機確認
- ライブ映像で通常案内、A選択案内、`M→H`、`A→H`を実機確認
- ライブ映像を大画面側の`x=2595, y=207`へ配置し、各キー操作後も位置が変わらないことを確認
- ライブ映像開始時に位置・サイズ・最大化状態をリセットする処理を撤去
- 起動時の収まり判定を、主画面固定から現在ウィンドウがあるディスプレイ基準へ変更
- Releaseビルド: 成功、警告0、エラー0
- 通常セルフテスト: 15件成功
- 位置保持修正版の実行物は`artifacts/publish/host-test-position`
- 位置保持修正版を大画面側の`x=2606, y=216、365×396`で起動
- ライブ映像への切替後も`x=2606, y=216、365×396`のままで、位置とサイズが変わらないことを目視確認

## 2026-07-30 Space操作の削除

- 元のMac版から継承したSpaceの中央タップとダブルタップには、現在の利用者向け仕様で明確な用途がないことを確認
- マウス操作で代用でき、単発Spaceにも350ミリ秒の待ち時間が発生するため、Windows版のキー割り当て、待機処理、ADBダブルタップ処理を削除
- Mac版はWindows上でソースを変更せず、修正指示書へ削除対象を記載
- 2026-07-29のSpace試験記録と手動確認待ちは履歴として残すが、現行仕様の合格条件には含めない
- Space削除後のReleaseビルドは警告0、エラー0、セルフテスト15件成功

## 2026-07-30 Mac版アイコンの適用

- Mac版の`RokidGlasses.icns`を元画像として使用し、Windows用に16、20、24、32、40、48、64、128、256pxを収録したICOを作成
- WindowsアプリのEXEリソースとメインウインドウのタイトルバーへ同じICOを設定
- Releaseビルドは警告0、エラー0、セルフテスト15件成功
- Release版EXEから埋め込みアイコンを取得し、Mac版と同じグラスとカーソルのデザインであることを画像確認

## 2026-07-30 ADB接続方式の自動変更撤去

- 起動待機中にUSB Rokidを発見した場合、Wi-Fi復旧処理へ入らずUSBシリアルをそのまま使用するよう変更
- Wi-Fi設定画面の自動操作、`wlan0`アドレス待機、`adb tcpip 5555`、固定5555番ポートへの接続作成をWindowsコードから削除
- USBシリアルをWi-Fiアドレス保存ファイルへ書き込まない
- 待機中にUSBが現れる模擬ADB試験を追加し、USB接続の維持、tcpip未実行、Wi-Fiアドレス未保存を確認
- Releaseビルドは警告0、エラー0、セルフテスト16件成功

## 2026-07-30 USBから暗号化Wi-Fiへの移行

- 「初回だけUSBケーブルを接続し、その後は無線で使用する」というMac版と共通の利用者向け仕様を再確認
- USBでRokidを確認後、Rokid独自のWi-Fi有効設定、AndroidのWi-Fi、Android標準の`adb_wifi_enabled`を順に有効化
- `_adb-tls-connect._tcp`が通知する動的ポートへ接続し、Rokidであることを再確認してから保存・切り替える
- 15秒以内に暗号化Wi-Fiを確認できない場合はUSBを継続使用する
- 固定5555番ポートと`adb tcpip 5555`は引き続き使用しない
- 暗号化Wi-Fi移行とUSB継続の模擬ADB試験を追加
- Releaseビルドは警告0、エラー0、セルフテスト17件成功
- Windows側の既存Wi-Fi ADB接続だけを切断し、USB接続中のRV101へ製品と同じ接続処理を実行
- USBからmDNSで通知された動的TLS接続先へ移行し、Rokidであることの再確認と接続先保存に成功
- 実機移行試験を含むセルフテスト18件成功
- USBケーブルを外した後、USB機器が一覧から消え、無線接続だけで`device`状態、Rokid RG-glasses、480×640の画面データ受信を確認
- 固有のIPアドレス、動的ポート、USBシリアル番号は試験記録へ保存しない

## 2026-07-30 TLS接続の最終再点検

- 初回起動だけでなく、自動再接続中にUSBを発見した場合も同じUSB→動的TLS移行処理を通すよう修正
- 保存済み、直前、`adb devices`の固定5555番接続を切断・削除し、再利用しない処理を追加
- Sandbox試験用起動スクリプトも固定5555番接続を候補から除外
- 再接続時のUSB→TLS移行と、旧5555番接続から動的TLSへの置換を模擬ADBで確認
- Releaseビルドは警告0、エラー0、セルフテスト19件成功
- 実機でTLS無線をいったん切断し、USBへ退避した再接続処理から動的TLSへ戻ることを確認
- 再接続後にUSBケーブルを外し、無線接続だけで`device`状態、Rokid RG-glasses、480×640の画面データ受信を確認
- 実機再接続試験を含むセルフテスト20件成功

## 2026-07-30 公開前確認の完了

- 背景なし画面とライブ映像で、H/M/A、アプリ選択、Esc、マウス操作、入力隔離を実機確認済み
- ライブ映像ウインドウを利用者が配置した大画面へ維持し、キー操作後も位置とサイズが変わらないことを確認
- Spaceの単発・ダブルタップは不要と判断して現行仕様から削除済み
- USBからTLS無線への初回移行、再接続時のUSB退避からTLS復帰、USB抜線後の画面受信を実機確認済み
- 利用者がWindows版の完成とGitHub公開を明示承認
- T13とT18をPASSへ更新
